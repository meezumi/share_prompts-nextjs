
// this loading.js will handle the loading Animation, till the page.js components are all loaded Up.

const Loading = () => {
  // we can add own UI inside, including a skeleton.
  return <LoadingSkeleton/>
}

export default loading;